package com.example.spendtracker.ui.testing;

/** Debug-only UI test host; it never initializes a transaction repository or requests SMS access. */
public class TransactionInteractionTestActivity extends androidx.appcompat.app.AppCompatActivity { }
